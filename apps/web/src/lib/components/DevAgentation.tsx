import { Agentation } from "agentation";

/**
 * Dev-only visual annotation toolbar ([agentation](https://github.com/benjitaylor/agentation)).
 * Click elements, add notes, copy structured selectors for agents.
 */
export function DevAgentation() {
  return <Agentation />;
}
