Chrome extension is almost fully built, and for the platform got the quick tasks done but projects and rest is still remaining, I think I might have something to share in a few weeks so yh its good so far

sharing designs

test for ai:
create a hotel booking page for available homes
I want to see the num of rooms available
types of rooms available
the specialisms
the acceptance rating

add states/steps to sessions, so you can hit undo and revert your changes.

queue - sessions
task - streaming
tool - anything
file tree - sessions

add file upload support

face hash npm

Chrome extension

- add record option to toolbar
- annotations should delete when PR is merged

- add record user flow option to chrome extension - automate this too

- add sandbox automation with GitHub actions
- create mcp that interacts with ai sdk

improve ui of logs, see how automaker did it here
https://automaker.app/#features

send for code review should:

1. mark as code review ready
2. eva does testing audit
3. eva does code review
4. eva does accessibility audit

https://x.com/DBVolkov/status/2018345929968144706/photo/1
add a dev-insights page with automations

- summarise yesterdays git activity for standup
- create weekly update of new changes made live / have been pushed to staging

ask user question tool
https://www.atcyrus.com/stories/claude-code-ask-user-question-tool-guide

in testing area we have 2 tabs

- ui frontend end to end testing - which creates a 2 row layout with Eva
  clicking through the ui and outputting logs
  e.g. https://x.com/i/status/2017975473641308352
- code testing - which evaluates code against documents

ingest streaming
add doc on company's workflow so eva knows and can suggest improvements

- stream json
- json to ui
- claude code streaming response in sandbox
- ai elemnets
- preview tab

- take screenshot or clip of change made in task as proof after running
- create mcp server inside sandbox for it to run one off queries in analyse
- create reports, generate charts, etc
- just make mcp server and then add connector on claude cowork, idk if it's worth to have a separate research page in eva
- mcp for ask mode too :/
- rename "web" to "carepulse"

Eva Design
one mistake during TE development was that there were new designs for the outcome selector, which meant that we had to make new components. What if design inspiration came from existing components in the codebase so that we can make them more reusable across the codebase and make minimal changes. E.g. could prompt the AI to generate designs as inspiration for a project, which then searches the codebase and bases the design based on existing implementation - this would mean new designs would match current designs and make them easier to maintain (which would also be better for AI).

Eva Teams
is it possible to build an AI agent that has access to everything on SharePoint/Teams files I want an AI agent to join a teams call, it remains on mute but listens to everything we say, if a question is mentioned during the call, she can raise her hand, we then say "yes eva", she then unmutes and starts speaking and answering the questions

Pulse Core

- add option to create one big task in feature or split tasks and implement separately
- add option to add task manually to a feature (ie for any fixes)
- add button to "Build feature" in features page, (it auto starts tasks and moves them to code review status), show pop-up warning of what it does
- add sub tasks to task which the user can add too
- add button to "Create and Start task" when creating task
- add setting to choose model
- add toggle to add tests for each task or overall test or no test
- see ralphy config, copy that
- each change made in a session = new commit made to the branch. 1 message = 1 commit, 1 session = 1 branch
- add edit task option to each task generated by the plan, so can fix wording instead of having to interview
- in plan, add a tab for requirements, user can enter requirements, this creates a task to compare feature implementation against requirements and make sure it matches
- once branch in session has been merged, the session is archived.

Pulse Assist

- this should just a chat UI
- 2 modes: Ask / Create Issue
- Ask just retrieves current page URL, searches codebase for code at the route, returns answer based on user query
- Create Issue mode is where the select option appears, the user can select element, and write the issue, this will create a task and flag

Pulse Analyse/Research

- add convex mcp to the sandbox so you can query the data in the ask mode, only enable querying and not mutating or anything like that
- can call v2/v3 apis to fetch data in sandbox
- not just answer questions but create exports, cqc export can be done through natural language, we can create routines to have this done automatically every day at 9am

Test Arena

- test PRs, or entire codebase against the business logic defined in docs
- 1 doc = 1 agent = 1 test

change routing structure. it should be set up like this instead:
/[repo]
/plan
/features
/[featureId]
/quick-tasks

add GitHub repo switcher at top, so should be like a dropdown component from heroui

add plan tab in sidebar. on this page you paste in the requirements, it will ask a bunch of questions, then create a spec, like 1-10 of todos. it will then create a feature with those 10 todos tasks stored under it with all the details. before creating the feature the user should be prompted to either save plan as draft or start creating feature. then on this plan tab page it just shows the list of plans.

this plan page is not a Kanban page, it's a conversation, a spec generator. also make it so the tasks can depend on each other and cannot run until the task it depends on has completed. this prevents the agent working on a task that is missing a previous implementation.

after a plan is created, in the list of plans, add a button to interview the plan, so it asks the user a bunch more questions based on the plan to increase its length / update requirements. plans have 3 states - draft (in progress conversation + editable, no tasks generated yet), finalised plan (spec is locked, tasks are generated, just need to hit create feature), feature-created (plan can't be changed, features exist, tasks have been created, execution can begin).

features tab in sidebar. clicking on features shows a list of features, clicking on the feature takes you to the feature/[id]/page.tsx which has the Kanban board for this of auto generated tasks from 1-10 as generated by the plan. btw each task can have subtasks too, but these are more simple, like bullet points of checklists for the task to be completed. so if there is 10 tasks, a task can have 2.1, 2.2, 2.3, etc. each task will have their own GitHub branch but it will be related to the feature. so the feature will make the base branch called conductor/feature-test, then each task will make a branch called conductor/feature-test-{taskNumber}, so then each PR can merged into each other into the base branch which acts as a completed feature. for the schema of a feature, also make sure you include the user of who created the feature and a status string field. Subtasks are documentation only - Agents do not run per subtask. They are used for: Completion criteria, Verification, Review only. model task dependencies like dependencies: Id<"tasks">[]

Quick tasks tab in sidebar. this page is just a kanban board. this is for quick tasks, the user can click Add task and just enter the prompt or issue and it will create just 1 todo in the Kanban board for that task.

sidebar should show active tasks in an accordion (this is collapsible)

a task in both features/quick tasks should have 6 different states, archived, backlog, to do, in progress, code review, done. it is in code review when agent has completed the task. it is done when the PR has been merged.

for the tech stack we are using convex for the db, openrouter (use ai sdk integration) for the spec/plan/task generation. then for the actual llm ai agent generation I will use Modal but we will set that up later - ignore this for now.

ultrathink Great now I need you to work on the plan part of the codebase again. When creating a plan, you paste in the requirements. It then goes to the page and automatically starts asking questions. I think on this page we should have tabs - Plan and Chat. In the chat page it will ask about 10 or so questions before it creates a detailed plan which is visible in the Plan tab. Use heroui for the Tabs. The plan should contain the Feature name and the tasks and what each task depends on. The plan should be broken up into 10 tasks max. Then on the Plan tab, there will be a button to Create Feature, which it then creates a feature from and creates a task per whats written in the plan. Also after plan is generated, in the Plan tab, along with the Create Feature button there should be an Interview button, which takes you back to the Chat tab, asks 10 more questions to clarify/expand details for the plan so it's more explicit. In both cases the questions it asks should be a multiple choice question so it's easier on the user. Until the create feature button is clicked, a plan is in the draft state. If a plan is generated but feature not clicked/generated it is in finalised state. If no plan then it is in draft state. add a badge to display this state on the plans list page.

ultrathink and come up with a list of todos to implement the above please

daytona
0.00001400

e2b
0.000028/s

modal
0.0000131/s

vercel
0.0000355/s
